#include "main.h"

/**
 *
 *
 *
 */

int _printf(const char *format, ...)
{
	int cont, arg;
	va_list list;

	for (cont = 0; format[cont] != '\0'; cont++)
	{
		if (format[cont] == '%')
		{
			arg = (*get_func(format[cont + 1](int);
		}




}
